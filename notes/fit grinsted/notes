other possible constraints
    std error in observed data

    2 meter limitation
        # impose a maximum SLR constraint for the observed period
        if (max(modelGmsl[, 2]) - min(modelGmsl[, 2]) > 2) {
            return (Inf)
        }

Grinsted's fit:
    foo<-grinsted(gmst, times)
    sse(foo,gmsl)
    [1] 0.1480596

# with Euler, it is a nice compromise to use the monthly values;
# this gets us closer to ode45 or ode23
#gmst <- ar4bias(loadHadcrut()[[2]], 12)$ar4gmsx
