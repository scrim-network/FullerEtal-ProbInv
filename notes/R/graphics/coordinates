converting between text size and user coordinates

    # calculate height of text in user coordinates
    #strwidth, strheight, par("cxy")
    #textHeight <- par("usr")[3] - grconvertY(par("ps")/72, from="inches", to="user")


    # reserve lines to use outer=T for the lower axis and label;
    # allows using 0.5 in par(fig) and getting equally sized plots;
    # one line is 0.2 inches:  par("csi") or par("cin")[2]
    #
    #par(oma=c(4, 1, 1, 1))
    par(omi=c(0.75, 0.25, 0.25, 0.25))


convert between inches and plotting area
    fin, pin

general conversion
    grconvertX()
    grconvertY()

length is inches in arrow
    arrows(assimYear + 5, arrowx, assimYear + 50, lwd=2, length=0.1)
