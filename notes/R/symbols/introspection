class() vs. typeof()

    grin (DEoptim)
        sapply(sapply(lsj(), get), class)
          endYear     grctx      grin startYear
        "numeric"    "list" "DEoptim" "numeric"

        typeof(grin)
        [1] "list"

        attributes(grin)
            $names
            [1] "optim"  "member"

            $class
            [1] "DEoptim"

    function (rm)
        class(rm)
        [1] "function"

        typeof(rm)
        [1] "closure"

        attributes(rm)
        NULL

also see mode()

as.name("foo") == as.name("foo")

all.names(of call object)
all.vars?
