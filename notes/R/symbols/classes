unclass(foo) removes class attributes

show the methods defined for a specific class
    methods(class="function")
    methods(class="data.frame")

show all the classes defining the plot method
    methods(plot)
    methods(print)
    methods(summary)

generic methods -- UseMethod does method dispatch
    > coef
    function (object, ...)
    UseMethod("coef")

    coef is a function not valid for atomic vectors?
    Error in object$coefficients : $ operator is invalid for atomic vectors

getting around "Non-visible functions are asterisked"
    getAnywhere("coef.aov")
    getS3method("coef", "aov")

example of "old" S3 style from docs
    require(stats)
    setOldClass(c("mlm", "lm"))
    setGeneric("dfResidual", function(model)standardGeneric("dfResidual"))
    setMethod("dfResidual", "lm", function(model)model$df.residual)

    ## dfResidual will work on mlm objects as well as lm objects
    myData <- data.frame(time = 1:10, y = (1:10)^.5)
    myLm <- lm(cbind(y, y^3)  ~ time, myData)

    rm(myData, myLm)
    removeGeneric("dfResidual")
    ## Not run: setOldClass("data.frame", prototoype = data.frame())

    ## End(Not run)

related funcitons
    setGeneric?  setMethod?  setClass?
    standardGeneric(f)
