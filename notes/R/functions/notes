match.fun -- takes function name or symbol, and returns function

do.call -- takes function name as string or symbol and list() of parameters

call -- creates unevaluated function call object;  list can be coered to call (parameters as list members?) -- as.call
    execute call with eval

can make call like this as well:  "[<-"(`*tmp*`, 3:5, value=13:15)
    BUT, cannot make call like this:  foo <- "ls"; foo()

    instead, match.fun("ls")()
