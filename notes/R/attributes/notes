display attributes
    attributes(obj)

assign attributes
    attr(z, "dim") <- c(10,10)

example of user-defined attribute
    attr(bar, "TOADS") <- "field"
    attr(bar, "TOADS")
    [1] "field"

very LISP-ish...  objects are lists


kinds of attributes:
    names, dimnames
    dim
    list member names ($names)
    levels of factors
    class of object
