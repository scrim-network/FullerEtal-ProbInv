->email Klaus photos and ask for PPT again

code
    *run Fortran model

    update model

    *fix gamma prior

    try adaptive MCMC (see DOECLIM_calib_driver among others)

->ask Alex for paper
    climatic change, in review

    read Bamber Nature paper cited by Alex

kelsey -- link to paper and inverse gamma stuff
    Kelsey's paper appendix--different windows--Table 1

woju -- daisF model -- Sneasy Makefile, DOEclim/Sneasy
    also see bitbucket stuff

compare code
    ssh woju
    diff -r /woju/s0/klr324/Ruckertetal_DAIS_codes/ ~/Ruckertetal_DAIS_codes/

    rsync -n --stats -hh -v -H -x -a --delete-after --exclude .git/ woju.scrim.psu.edu:/woju/s0/klr324/Ruckertetal_DAIS_codes/ ~/Ruckertetal_DAIS_codes.ORIG

    rsync -n --stats -hh -v -H -x -a --delete-after --exclude Random_out/ --exclude Workspace --exclude DAIS_matlab/OtimizedInitialParameters.csv --exclude DAIS_matlab/old_DataCode_versions/DAIS_Matlab_MCMCcalibration.mat --exclude old_code_attempts/dais_error.txt --exclude .git/ woju.scrim.psu.edu:/woju/s0/klr324/Ruckertetal_DAIS_codes ~/Ruckertetal_DAIS_codes.ORIG

    ~/src/ruckert_dais/
